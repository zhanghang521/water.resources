declare type deptDataType = {
    dId:number,
    dName:string,
    dCount:string,
    dAddr:string,
    uId:number
}

declare type deptType ={
    dName:string | null,
    dCount:string | null,
    dAddr:string | null,
    uId:string | null
}
declare type RuleForm ={
    dName:string,
    dCount:string,
    dAddr:string,
    uId:string
}