export interface menuDataType {
    nId: number
    nName: string
    nLevel: string
    nUrl: string
    nPid: string
  }
  
export interface MenuType {
    nName: string | null
    nLevel: string | null
    nUrl: string | null
  }

  export interface RuleForm {
    nName: string
    nUrl: string
    nLevel: string
}